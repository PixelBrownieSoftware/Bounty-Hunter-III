using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class o_npcharacter : BHIII_character
{

	new void Start () {
        base.Start();
        terminalspd = 30f;
        Initialize();
	}

    IEnumerator SHOOT()
    {
        while (true)
        {

            yield return new WaitForSeconds(0.5f);
        }
    }


    new void Update()
    {
        base.Update();
        

        if (target != null)
        {
            CHARACTER_STATE = CHARACTER_STATES.STATE_MOVING;
        }
        switch (CHARACTER_STATE)
        {
            case CHARACTER_STATES.STATE_MOVING:
                if (target != null)
                {
                    xvel = LookAtTarget(target).x * 6;
                    yvel = LookAtTarget(target).y * 6;

                }
                if (xvel != 0 || yvel != 0)
                {
                    velocity += new Vector2(xvel, 0);
                    velocity += new Vector2(0, yvel);
                }
                break;

        }
    }
}
*/